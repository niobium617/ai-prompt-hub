<view class="container">
  <view wx:if="{{!favorites.length}}" class="empty-state">
    <text class="icon">📭</text><text>暂无收藏</text>
  </view>
  <prompt-card wx:for="{{favorites}}" wx:key="id" prompt="{{item}}" />
</view>
